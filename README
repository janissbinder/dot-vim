Installation:

  git clone https://github.com/janissbinder/dot-vim.git ~/.vim
  cd ~/vim && bash init.sh
  sudo apt install vim-gtk3 # for easy copy paste and stuff

  Open vim and enter the following commands

  :source ~/.vimrc
  :PlugInstall
